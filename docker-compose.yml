services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "3002:8080"
    environment:
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:?Set OPENROUTER_API_KEY in the environment or .env}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-}
