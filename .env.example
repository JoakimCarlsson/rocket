# Optional. Without a key the app uses the built-in local engineer.
OPENROUTER_API_KEY=
# Optional model override (defaults to bytedance-seed/seed-2.0-mini). Must support structured outputs.
OPENROUTER_MODEL=
